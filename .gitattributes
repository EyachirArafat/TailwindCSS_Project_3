# Ensure all text files have LF line endings
* text=auto eol=lf

# Force LF for specific file types (e.g., JavaScript, JSON, CSS)
*.js text eol=lf
*.json text eol=lf
*.css text eol=lf

# Set LF line endings for the .gitattributes file itself
.gitattributes text eol=lf
